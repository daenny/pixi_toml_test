[project]
name = "repro_dep"
version = "3.1.0"
description = "Dependency package used to check which git branch pixi checks out"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
