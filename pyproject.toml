[project]
dynamic = ["name", "version", "authors", "license", "dependencies"]
keywords = ["test"]
scripts = { test_toml = 'pixi_toml_test:main' }

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.metadata.hooks.custom]
path = "scripts/hatch_build.py"
